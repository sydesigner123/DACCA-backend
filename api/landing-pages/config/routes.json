{
  "routes": [
    {
      "method": "GET",
      "path": "/landing-pages",
      "handler": "landing-pages.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/landing-pages/count",
      "handler": "landing-pages.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/landing-pages/:id",
      "handler": "landing-pages.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/landing-pages",
      "handler": "landing-pages.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/landing-pages/:id",
      "handler": "landing-pages.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/landing-pages/:id",
      "handler": "landing-pages.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
